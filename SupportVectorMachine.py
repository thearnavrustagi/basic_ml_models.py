import numpy as np

class SVM (object):
    """
    This unlike LinearRegression, tries to find the most optimal solution based
    on only the "difficult points" near the decision boundary
    """
    def __init__ (self,features : np.ndarray, target : np.ndarray):
        self.features = features
        self.target = target
   
    def __call__ ():
        pass
